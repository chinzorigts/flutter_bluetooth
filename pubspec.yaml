name: flutter_bluetooth
description: Bluetooth plugins test project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  get: ^4.6.1
  getwidget: ^2.0.5
  flutter_blue_plus: ^1.1.3
  flutter_launcher_icons_maker: ^0.10.2
  lottie: ^1.3.0
  package_info_plus: ^1.4.2
  device_info_plus: ^3.2.3
  flutter_smart_dialog: ^3.3.9


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter_icons:
  android: true
  ios: true
  # adaptive_icon_background: "assets/images/ic_launcher_adaptive_back.png"
  # adaptive_icon_foreground: "assets/images/ic_launcher_adaptive_fore.png"
  # adaptive_icon_foreground: "#39A9DB"
  image_path: "assets/images/bluetooth.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/lotties/
